name: Depot Build with VNC Access
on: workflow_dispatch

jobs:
  build:
    runs-on: macos-14
    permissions:
      contents: read
      id-token: write
      packages: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # --- بخش مربوط به بیلد Depot ---
      - name: Install Depot CLI
        run: brew install depot/tap/depot

      - uses: depot/setup-action@v1
      
      - name: Depot Build
        uses: depot/build-push-action@v1
        with:
          project: 2hwk5n6kr1
          context: .

      # --- بخش فیکس شده VNC و دسترسی از راه دور ---
      - name: Enable Remote Desktop (Fix Kickstart Error)
        run: |
          # فعال‌سازی Screen Sharing با استفاده از تنظیمات سیستم
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCAlwaysStartOnConsole -bool true
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement ARD_AllLocalUsers -bool true
          
          # تنظیم پسورد VNC به روش مطمئن‌تر (Password: 123456)
          echo "123456" | perl -we 'BEGIN { @k = unpack "C*", pack "H*", "1734516E8BA8C5E2FF1C39567390ADCA"}; chomp(my $p = <STDIN>); print pack "C*", map { $p =~ s/(.)//s ? ord($1) ^ shift(@k) : shift(@k) } 0..15' > /tmp/.vncpasswd
          sudo mkdir -p /Library/Application\ Support/Apple/Remote\ Desktop/
          sudo cp /tmp/.vncpasswd /Library/Application\ Support/Apple/Remote\ Desktop/VNCConfig.passwd
          
          # شروع سرویس
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true

      - name: Install Networking Tools (noVNC & Cloudflared)
        run: brew install novnc cloudflared

      - name: Start Web Access Tunnel
        run: |
          # اجرای noVNC برای تبدیل VNC به وب
          websockify --web /usr/local/share/novnc/ --detach 6080 localhost:5900
          
          echo "------------------------------------------"
          echo "LİNK CLOUDFLARE RA DAR LOG ZIR PEYDA KONID:"
          cloudflared tunnel --url http://localhost:6080 &
          echo "------------------------------------------"
          
          # زمان برای بالا آمدن تونل
          sleep 15

      - name: Keep Alive
        run: |
          echo "VNC Password: 123456"
          # رانر را برای کار با محیط گرافیکی باز نگه می‌دارد
          sleep 21600
          
