name: macOS Depot Build & Remote VNC
on: workflow_dispatch

jobs:
  build:
    name: "M4 Pro Infrastructure Simulation"
    runs-on: macos-14
    permissions:
      contents: read
      id-token: write
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # ۱. نصب پیش‌نیازها (اصلاح شده)
      - name: Install Networking Tools
        run: |
          brew install novnc cloudflared
          # پیدا کردن مسیر دقیق نصب noVNC برای جلوگیری از ارور مسیر
          echo "NOVNC_PATH=$(brew --prefix novnc)/libexec/novnc" >> $GITHUB_ENV

      # ۲. فعال‌سازی VNC (روش سازگار با macOS 14)
      - name: Enable Remote Desktop
        run: |
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCAlwaysStartOnConsole -bool true
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement ARD_AllLocalUsers -bool true
          
          # تنظیم رمز عبور: 123456
          echo "123456" | perl -we 'BEGIN { @k = unpack "C*", pack "H*", "1734516E8BA8C5E2FF1C39567390ADCA"}; chomp(my $p = <STDIN>); print pack "C*", map { $p =~ s/(.)//s ? ord($1) ^ shift(@k) : shift(@k) } 0..15' > /tmp/.vncpasswd
          sudo mkdir -p /Library/Application\ Support/Apple/Remote\ Desktop/
          sudo cp /tmp/.vncpasswd /Library/Application\ Support/Apple/Remote\ Desktop/VNCConfig.passwd
          
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true

      # ۳. اجرای تونل کلودفلر و noVNC
      - name: Start Web Access Tunnel
        run: |
          # استفاده از مسیر صحیح novnc که در مرحله قبل ذخیره شد
          # پورت 5900 مک به 6080 وب تبدیل می‌شود
          websockify --web ${{ env.NOVNC_PATH }} --detach 6080 localhost:5900
          
          echo "------------------------------------------------"
          echo "LOOK FOR CLOUDFLARE URL BELOW:"
          cloudflared tunnel --url http://localhost:6080 &
          echo "------------------------------------------------"
          sleep 15

      # ۴. نمایش فایل‌ها برای اطمینان از وجود Dockerfile
      - name: Debug Directory
        run: ls -R

      # ۵. اجرای Depot
      - name: Install Depot CLI
        run: brew install depot/tap/depot

      - name: Setup Depot
        uses: depot/setup-action@v1

      - name: Depot Build
        uses: depot/build-push-action@v1
        with:
          project: 2hwk5n6kr1
          context: .
          push: false

      # ۶. زنده نگه داشتن سیستم
      - name: Keep Alive
        run: |
          echo "VNC Password: 123456"
          sleep 21600
          
