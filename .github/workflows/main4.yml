name: macOS Depot Build & Remote VNC
on: workflow_dispatch

jobs:
  build:
    name: "M4 Pro Infrastructure Simulation"
    runs-on: macos-14
    permissions:
      contents: read
      id-token: write
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # ۱. نصب ابزارهای شبکه و VNC
      - name: Install Networking Tools
        run: |
          brew install cloudflared
          git clone https://github.com/novnc/noVNC.git /tmp/novnc
          python3 -m venv $GITHUB_WORKSPACE/venv
          $GITHUB_WORKSPACE/venv/bin/pip install websockify

      # ۲. فعال‌سازی VNC با متد kickstart (حل مشکل پسورد و یوزرنیم)
      - name: Enable Remote Desktop
        run: |
          # تنظیم پسورد برای یوزر runner (در صورت نیاز به ورود در صفحه لاگین مک)
          sudo dscl . -passwd /Users/runner password123
          
          # فعال‌سازی Remote Management و VNC با پسورد مشخص
          # پسورد VNC باید دقیقاً با فلگ -vncpasswd تنظیم شود
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure -access -on \
            -clientopts -setvncpasswd -vncpasswd password123 \
            -restart -agent -privs -all
          
          echo "✅ VNC با رمز 'password123' فعال شد."

      # ۳. اجرای تونل کلودفلر و noVNC
      - name: Start Web Access Tunnel
        run: |
          $GITHUB_WORKSPACE/venv/bin/python3 -m websockify --web /tmp/novnc 6080 localhost:5900 > websockify.log 2>&1 &
          echo "------------------------------------------------"
          echo "👇 لینک ورود به محیط مک:"
          cloudflared tunnel --url http://localhost:6080 &
          echo "------------------------------------------------"
          sleep 15

      # ۴. پیدا کردن هوشمند Dockerfile
      - name: Find Dockerfile
        id: find-dockerfile
        run: |
          DOCKERFILE_PATH=$(find . -name "Dockerfile" -o -name "dockerfile" | head -n 1)
          if [ -z "$DOCKERFILE_PATH" ]; then
            echo "FROM alpine" > Dockerfile
            echo "path=./Dockerfile" >> $GITHUB_OUTPUT
          else
            echo "path=$DOCKERFILE_PATH" >> $GITHUB_OUTPUT
          fi

      # ۵. تنظیمات داکر (فیک) برای لاگین در GHCR
      - name: Manual Registry Auth
        run: |
          mkdir -p ~/.docker
          AUTH=$(echo -n "${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}" | base64)
          echo "{\"auths\":{\"ghcr.io\":{\"auth\":\"$AUTH\"}}}" > ~/.docker/config.json

      # ۶. نصب Depot CLI و بیلد
      - name: Install Depot CLI
        run: brew install depot/tap/depot

      - name: Depot Build and Push
        run: |
          depot build . \
            --project 2hwk5n6kr1 \
            --file ${{ steps.find-dockerfile.outputs.path }} \
            --push \
            --tag ghcr.io/${{ github.repository_owner }}/m4-sim-image:latest
        env:
          DEPOT_TOKEN: ${{ secrets.DEPOT_TOKEN }} 

      # ۷. زنده نگه داشتن سیستم
      - name: Keep Alive
        run: |
          echo "✅ سیستم با موفقیت بالا آمد."
          echo "لینک کلودفلر را باز کنید."
          echo "در کادر Password عبارت password123 را وارد کنید."
          echo "اگر صفحه لاگین مک را دیدید: User: runner | Pass: password123"
          sleep 21600
          
