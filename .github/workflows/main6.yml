name: Hosted macOS 16vCPU (Native VNC for Manual Setup)

on:
  workflow_dispatch:

jobs:
  macos-desktop:
    runs-on: namespace-profile-samiyarbig # رانر اختصاصی شما

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ۱. اتصال به شبکه امن Tailscale
      - name: Install and Connect Tailscale
        run: |
          echo "==== Installing Tailscale ===="
          brew install tailscale
          sudo brew services start tailscale
          sudo ln -s /opt/homebrew/bin/tailscale /usr/local/bin/tailscale || true
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTHKEY }} --accept-routes

      # ۲. ساخت یک کاربر ادمین جدید به نام samiyar (برای لاگین در محیط گرافیکی)
      - name: Create Dedicated Admin User
        run: |
          echo "==== Creating Remote User 'samiyar' ===="
          sudo dscl . -delete /Users/samiyar || true
          
          sudo dscl . -create /Users/samiyar
          sudo dscl . -create /Users/samiyar UserShell /bin/zsh
          sudo dscl . -create /Users/samiyar RealName "Samiyar Big"
          sudo dscl . -create /Users/samiyar UniqueID "555"
          sudo dscl . -create /Users/samiyar PrimaryGroupID 20
          sudo dscl . -create /Users/samiyar NFSHomeDirectory /Users/samiyar
          
          # ست کردن رمز عبور ثابت
          sudo dscl . -passwd /Users/samiyar mac2026!
          
          # ارتقا به سطح دسترسی ادمین
          sudo dscl . -append /Groups/admin GroupMembership samiyar
          
          # ساخت دایرکتوری خانگی
          sudo mkdir -p /Users/samiyar
          sudo chown -R samiyar:staff /Users/samiyar
          
          # جلوگیری از به خواب رفتن گرافیک و سیستم
          sudo pmset -a displaysleep 0 sleep 0 disksleep 0 || true

      # ۳. فعال‌سازی ریموت دسکتاپ بومی اپل (VNC) با دستورات تصحیح‌شده مک ۲۶ Tahoe
      - name: Enable Native Apple Remote Desktop & VNC
        run: |
          echo "==== Activating Apple Remote Management ===="
          
          # فعال‌سازی دیمون ریموت دسکتاپ
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate
          
          # تنظیم دسترسی کامل برای همه کاربران ادمین
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -configure -allowAccessFor -allUsers -privs -all
          
          # ست کردن پسورد VNC با آرگومان‌های بومی و بدون ارور
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -configure -clientopts -setvnclegacy yes -setvncpw yes -vncpw mac2026!
          
          # ری‌استارت نهایی سرویس گرافیکی ریموت
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -restart -agent

      # ۴. نصب ابزارهای مدیریت و انتقال صدا
      - name: Install Audio Utilities via Homebrew
        run: |
          echo "==== Installing Audio Tools ===="
          brew install blackhole-2ch || true
          brew install switchaudio-osx || true
          
          sudo killall coreaudiod || true
          sleep 3
          
          # مسیردهی خروجی صدا به کارت صدای مجازی برای ضبط
          SwitchAudioSource -s "BlackHole 2ch" || true

      # ۵. زنده نگه داشتن فریم‌بافر دسکتاپ (جلوگیری از سیاه شدن مانیتور مجازی)
      - name: Keep Video Framebuffer Alive
        run: |
          echo "==== Preventing macOS Graphics from Sleeping ===="
          sudo caffeinate -u -d -i -s -t 7200 &
          sleep 2

      # ۶. نمایش اطلاعات ورود مستقیم
      - name: Get Connection Details
        run: |
          echo "==== Ready for Manual Setup via VNC ===="
          echo "------------------------------------------------"
          echo "Tailscale IP: $(tailscale ip -4)"
          echo "Username: samiyar"
          echo "Password: mac2026!"
          echo "VNC Password: mac2026!"
          echo "------------------------------------------------"
          echo "HOW TO CONNECT AND INSTALL:"
          echo "1. Open your VNC Viewer (like RealVNC or TightVNC) on your local system."
          echo "2. Connect to -> TAILSCALE_IP:5900"
          echo "3. Enter VNC Password: mac2026!"
          echo "4. Once inside the macOS GUI, open Safari, go to NoMachine's website, download and install it manually!"
          echo "------------------------------------------------"

      # ۷. زنده نگه داشتن رانر جهت کار شما
      - name: Keep Runner Alive
        run: |
          echo "==== Runner is alive and waiting for your VNC connection ===="
          sleep 7200
