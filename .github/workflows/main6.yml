name: Hosted macOS 16vCPU Environment (NoMachine)

on:
  workflow_dispatch:

jobs:
  macos-desktop:
    runs-on: avrea-macos-26-16-vcpu 

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ۱. راه‌اندازی شبکه Tailscale (امن‌ترین مسیر برای NoMachine)
      - name: Install and Connect Tailscale
        run: |
          echo "==== Installing Tailscale ===="
          brew install tailscale
          sudo brew services start tailscale
          sudo ln -s /opt/homebrew/bin/tailscale /usr/local/bin/tailscale || true
          
          echo "==== Connecting to Tailscale ===="
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTHKEY }} --accept-routes

      # ۲. ساخت کاربر برای مک و NoMachine
      - name: Create macOS User
        run: |
          echo "==== Creating GUI User ===="
          # ساخت یوزر ادمین جدید برای لاگین
          sudo sysadminctl -addUser nxuser -fullName "NoMachine Admin" -password "mac2026!" -admin
          sudo dscl . -create /Users/nxuser IsHidden 0
          
          # خاموش کردن خواب سیستم
          sudo pmset -a displaysleep 0 sleep 0 disksleep 0

      # ۳. نصب NoMachine از طریق Homebrew Cask
      - name: Install NoMachine
        run: |
          echo "==== Downloading and Installing NoMachine ===="
          brew install --cask nomachine

      # ۴. راه‌اندازی و ری‌استارت سرویس NX Server
      - name: Configure NoMachine Server
        run: |
          echo "==== Restarting NX Service ===="
          # اطمینان از اینکه سرویس بعد از نصب روی آی‌پی تیل‌اسکیل گوش می‌دهد
          sudo /etc/NX/nxserver --restart
          sudo /etc/NX/nxserver --status

      # ۵. بیدار نگه داشتن دیسپلی
      - name: Prevent Sleep
        run: |
          echo "==== Forcing macOS to stay awake ===="
          sudo caffeinate -disu &

      # ۶. اطلاعات اتصال
      - name: Get Connection Details
        run: |
          echo "==== Ready to Connect ===="
          echo "Tailscale IP: $(tailscale ip -4)"
          echo "Port: 4000 (Default)"
          echo "-----------------------------------"
          echo "Username: nxuser"
          echo "Password: mac2026!"

      # ۷. زنده نگه داشتن رانر
      - name: Keep Runner Alive
        run: |
          echo "Runner is perfectly running. Open NoMachine Player and connect!"
          sleep 7200
          
